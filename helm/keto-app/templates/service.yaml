apiVersion: v1
kind: Service
metadata:
  name: {{ include "keto-app.fullname" . }}
  labels:
    {{- include "keto-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "keto-app.selectorLabels" . | nindent 4 }}
